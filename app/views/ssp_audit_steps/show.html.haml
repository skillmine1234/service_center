.show_ticket
  .header-form-main{:style=>'background-color: #ecf5fa; width: 50%; height: 150px; display: inline-block;'}
    %h3 
      Customer Code:
      / = link_to @audit_step.customer_code, @audit_step.icol_customer
      = @audit_step.customer_code
    %h3 
      App Code:
      = @audit_step.app_code
    %h3
      Request Timestamp:
      = @audit_step.req_timestamp.try(:strftime, "%d/%m/%Y %I:%M%p")
    %h3
      Request Bitstream:
      = show_xml('Show Reply','request-link', @audit_step.req_bitstream)
  .header-form-extended{:style=>'background-color: #ecf5fa; width: 50%; height: 150px; display: inline-block;'}
    %h3
      Status:
      = @audit_step.status_code
      %tr
        - if @audit_step.status_code == "FAILED"
          = link_to "Fault Text", "#", :class => 'fault-link', :href => "javascript:void()", :style => "font-weight:100"
    %h3
      Step Name:
      = @audit_step.step_name
    %h3
      Reply Timestamp:
      = @audit_step.rep_timestamp.try(:strftime, "%d/%m/%Y %I:%M%p")
    %h3
      Reply Bitstream:
      = show_xml('Show Reply','reply-link', @audit_step.rep_bitstream)

  .header-form-main{:style=>'background-color: #F1F2F8; width: 50%; height: 450px; display: inline-block;'}
    #ticket-details
    %h3 Up Request Details:
    %table.table.no-border{:style=>"table-layout: fixed; width: 100%"}
      %tr
        %td.label Host
        %td.value
          = @audit_step.up_host
      %tr
        %td.label Request URI
        %td.value
          = @audit_step.up_req_uri
      %tr
        %td.label Request Headers
        %td.value
          = link_to "Show Header", "#", :class => 'val-req-header-link', :href => "javascript:void()", :style => "font-weight:100", data: {request: @audit_step.up_req_header }
      %tr
        %td.label Request Timestamp
        %td.value
          = @audit_step.up_req_timestamp.try(:strftime, "%d/%m/%Y %I:%M%p")
        %td
      %tr
        %td.label Request Bitstream
        %td.value
          = show_xml('Show Request','request-link',@audit_step.up_req_bitstream)
        %td

  .header-form-main{:style=>'background-color: #F1F2F8; width: 50%; height: 450px; display: inline-block;'}
    #ticket-details
    %h3 Up Reply Details:
    %table.table.no-border{:style=>"table-layout: fixed; width: 100%"}
      %tr
        %td.label Reply Headers
        %td.value
          = link_to "Show Header", "javascript:void()", :class => 'val-rep-header-link', :style => "font-weight:100", data: {reply: @audit_step.up_rep_header}
      %tr
        %td.label Reply Timestamp
        %td.value
          = @audit_step.up_rep_timestamp.try(:strftime, "%d/%m/%Y %I:%M%p")
        %td
      %tr
        %td.label Reply Bitstream
        %td.value
          = show_xml('Show Reply','reply-link',@audit_step.up_rep_bitstream)
        %td

= render partial: 'modal_boxes'

#faultText.modal.hide.fade{"aria-hidden" => "true", "aria-labelledby" => "myModalLabel", :role => "dialog", :tabindex => "-1"}
  .modal-header
    %button.close{"aria-hidden" => "true", "data-dismiss" => "modal", :type => "button"} ×
    %h3 Fault Text
    #error_message{:style => 'color:red'}
  .modal-body{:style => "max-height:450px;"}
    .demo-area
    %pre#fault_text{:style => 'word-wrap: break-word'}
    %table.table.no-border
      %tr
        %td.label Fault Code:
        %td= @audit_step.fault_code
      %tr
        %td.label Fault Code:
        %td= @audit_step.fault_subcode
      %tr
        %td.label Fault Reason:
        %td= @audit_step.fault_reason
