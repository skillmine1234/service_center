%h2.collapsible
  Advanced Search
  %span.caret
.collapsible-content
  = form_tag iam_organisations_path, :method => :put, :class => 'form-horizontal', :autocomplete=>"off" do
    .control-group
      = label_tag "Name", "Name", :class => 'control-label'
      = text_field_tag :name, params[:name]
    .control-group
      = label_tag "Organisation UUID", "Organisation UUID", :class => 'control-label'
      = text_field_tag :org_uuid, params[:org_uuid]
    = hidden_field_tag :page, params[:page] || 1
    .form-actions
      = submit_tag "Search", :name => nil, :class => 'btn btn-primary', :id => "adv_search_button"
      = button_tag "Reset", :type => 'button', :value=>'Reset', :class => 'btn', :id => 'adv_search_reset_button'

- if @searcher.errors.present?
  :javascript
    $('.collapsible-content').show(); 

= advanced_search_page_links
